# nwg-displays writes these at runtime (machine-specific monitor layout)
swaywm/sway/outputs
swaywm/sway/workspaces

# compiled build artifact (ship source, not the binary)
bin/lottogen

# private-only local tool (drafts articles from ncircular headlines via claude -p)
bin/newsdraft
